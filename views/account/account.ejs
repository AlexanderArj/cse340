<h1><%= title %></h1>

<%- messages() %>

<% if (typeof errors !== "undefined" && errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error => { %>
      <li><%= error.msg %></li>
    <% }) %>
  </ul>
<% } %>

<p>Logged in!</p>